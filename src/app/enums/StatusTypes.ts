export enum StatusTypes {
  draft = 'draft',
  active = 'active',
  removal = 'removal',
  archive = 'archive',
}

export enum StatusEnum {
  draft = 'Черновик',
  active = 'Активный',
  removal = 'На удаление',
  archive = 'Архивный',
}
export enum ShortStatusEnum {
  draft = 'Черновик',
  active = 'Активный',
}