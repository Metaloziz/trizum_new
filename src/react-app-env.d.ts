/// <reference types="react-scripts" />
interface React {
  readonly REACT_APP_URI: string;
  // readonly BASE_URL: string;
  // readonly REACT_APP_VERSION: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
